import { cleanup } from "@testing-library/react";
afterEach(cleanup);

describe("first", () => {
  it("should ", () => {
    // render(<Answer answer={null} handleClick={() => null} result={null} />);
  });
});
